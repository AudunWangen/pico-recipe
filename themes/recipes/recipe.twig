{% include ('header.twig') %}

    <div id="main" role="main">
        <div class="container">
            {{ content }}
            {% if meta.tags %}
                <div>Tagger: <ul class="tags">
                {% for tag in meta.tags %}
                    <li><a href="{{ base_url }}/tags/?tag={{ tag }}">{{ tag }}</a></li>
                {% endfor %}
                </ul></div>
            {% endif %}
        </div>
    </div>

{% include ('footer.twig') %}
